import logoImg from "../img/logo_horizontal_color.png";

const Logo = () => {
  return (
    <div className="navBar_links">
      <img src={logoImg} alt='"Logo' className="logoStyle"></img>
    </div>
  );
};

export default Logo;
